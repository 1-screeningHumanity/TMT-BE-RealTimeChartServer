spring:
  application:
    name: realtime-server

  data:
    redis:
      host: ${REDIS_DB}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
kis:
  key:
    socketKey: ${KIS_SOCKET_KEY}

kafka:
  bootstrap-server: ${KAFKA_SERVER}

server:
  port: 9000